package model;

import model.base.BasePage;
import org.openqa.selenium.WebDriver;

public class PlayTheGameAndLearnPage extends BasePage {
    protected PlayTheGameAndLearnPage(WebDriver driver) {
        super(driver);
    }
}
