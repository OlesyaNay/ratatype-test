package model;

import model.base.BasePage;
import org.openqa.selenium.WebDriver;

public class KeyboardTestPage extends BasePage {
    protected KeyboardTestPage(WebDriver driver) {
        super(driver);
    }
}
