package model;

import model.base.BasePage;
import org.openqa.selenium.WebDriver;

public class RemindPage extends BasePage {
    protected RemindPage(WebDriver driver) {
        super(driver);
    }
}
