package model;

import model.base.BasePage;
import org.openqa.selenium.WebDriver;

public class LearnPage extends BasePage {

    protected LearnPage(WebDriver driver) {
        super(driver);
    }
}
