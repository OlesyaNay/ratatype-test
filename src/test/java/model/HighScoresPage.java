package model;

import model.base.BasePage;
import org.openqa.selenium.WebDriver;

public class HighScoresPage extends BasePage {
    protected HighScoresPage(WebDriver driver) {
        super(driver);
    }
}
